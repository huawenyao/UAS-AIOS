# 切片 M11: audit

> 一次 AI coding 会话的完整输入。禁止读取 docs/strategic/design/ 整包。

## 0. 宪法（强制加载）
```yaml
# UAS-AIOS 宪法包（Agent 强制加载，禁止整包读取 design/）
version: "1.0"
status: frozen
adr: [ADR-SEL-001, ADR-SEL-002, ADR-SEL-003, ADR-EDH-001, ADR-EDH-002]

goals:
  G-1: 责任图为 L0 唯一经营写入
  G-2: 四剖面同一 Hub，场景不能写生产
  G-3: Agent 不直连系统，只经 MCP/cs.*
  G-4: 任务必带 source_node_id
  G-5: 内环一套、外环一套，禁第三套
  G-6: 个人记忆 ≠ 经营承诺

policy_order: [inject, tenant, registry, rbac, approval, gates, scope, execute, audit]
profiles: [scene, explore, builder, runtime]
tracks: [selfpaw, pipaw]
envelope: [tenant_id, actor_id, profile, track, correlation_id, idempotency_key, source_node_id]

never_outsource: [accountability_graph, world_model, law_pack, hub_control_plane, cs_contract, insight_task]
parts: [cube, graphiti, lethe, temporal, langgraph, mcp, a2a, osi]
no_os_substitutes: [Palantir, FabricIQ, Agentforce, CopilotStudio, Dify, Coze, CrewAI, AutoGen]

front_forbidden_nouns: [workflow_id, CubeQL, connector_id, neo4j, temporal_ui, langgraph, lethe]
trace: pack.open → insight.drill → task.issue → exec.open → pack.open

protocol_registry: configs/protocol/registry.json
port_spi: CubePort KgPort MemoryPort InnerLoopPort OuterLoopPort ConnectorPort McpPort SkillPort LawPort ArtifactPort BrokerPort EvolutionPort
```

## 1. 本模块契约
- protocol_id: `hub.audit.append`
- port: `AuditPort`
- decision: own · replaceable: False
- verbs: append, export
- errors: 无
- forbidden:
  - UPDATE 日志行
  - Cube 当审计
- 一线禁词: 审计

## 2. 验收标准
- [ ] 对齐 enterprise-audit-chain-spec
- [ ] A5 链完整

## 3. 红灯状态
- `test_audit_append_only`: missing
- `test_hash_chain`: missing
- `test_command_deck_not_audit`: missing
- `test_pack_open_renders_gate_node`: green
- `test_scene_cannot_invoke_cs_write`: green
- `test_task_requires_source_node`: green
- `test_ungrounded_cannot_issue`: green
- `test_exec_write_goes_through_connector_and_changes_kpi`: green
- `test_l2_waits_signal`: green
- `test_selfpaw_write_requires_escalation`: green
- `test_pipaw_memory_forbidden`: green
- `test_explore_cannot_execute_skill`: green
- `test_unapproved_law_not_compiled`: green
- `test_cross_tenant_forbidden`: green
- `test_thread_profile_immutable`: green

汇总: 12/15 green

## 4. 禁替代扫描（实现后必跑）
```bash
rg -i 'crewai|autogen|celery|camunda' services/ --type py
rg 'workflow_id|CubeQL' projects/aios-workstudio/demo/ || true
```

## 5. 上下文预算（只读这些）
- configs/protocol/KERNEL.yaml
- configs/protocol/registry.json（本模块节）
- harness\requirements\REQ-UAS-M11.req.md
- services/hub-api/uas_hub/ports.py（AuditPort）
- 禁止: docs/strategic/design/ 整包

## 6. DoR / DoD
- DoR: 红灯已写 · 契约已挂
- DoD: 红灯转绿 · 禁替代grep空 · state按轨迹更新 · AC勾选

---
完成度: AC 0/2 · 测试 12/15 · status=
判定: 完成 = 轨迹 green，不是卡片 green
